{
  "description": "Example for real Codex on git_worktree runs. Review carefully before using --sandbox workspace-write on a real repository.",
  "exec_worker_kind": "codex",
  "exec_argv": ["codex", "exec", "--sandbox", "workspace-write"],
  "workspace_strategy": "git_worktree"
}
