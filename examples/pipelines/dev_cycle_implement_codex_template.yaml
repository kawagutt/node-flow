# Development flow P2: stage 2 (implement) with real Codex CLI.
# TEMPLATE: replace argv with your environment's verified Codex invocation.
# `cat` is a placeholder command used to keep stdin wiring explicit in this template.
version: "1.5"
name: dev_cycle_implement_codex_template
inputs:
  approved_checkpoint_path: {}
  repo_root: {}
  base_ref: {}
  task_type: {}
graph:
  nodes:
    - id: implement
      type: workflows.development_flow.implement
      inputs:
        approved_checkpoint_path: ${inputs.approved_checkpoint_path}
        repo_root: ${inputs.repo_root}
        base_ref: ${inputs.base_ref}
        task_type: ${inputs.task_type}
      params:
        codex_exec:
          argv:
            - codex
            - exec
            - --model
            - REPLACE_ME
            - --
            - cat
        run_tests:
          argv: ["python3", "-m", "pytest", "-q"]
        write_checkpoint:
          checkpoint_dir: .nodeflow/checkpoints
          run_id: "002"
  final: implement
