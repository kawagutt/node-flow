# Development flow P2: stage 3 (review) with real Codex CLI.
# TEMPLATE: replace argv with your environment's verified Codex invocation.
# `cat` is a placeholder command used to keep stdin wiring explicit in this template.
version: "1.5"
name: dev_cycle_review_codex_template
inputs:
  approved_checkpoint_path: {}
  repo_root: {}
  base_ref: {}
  task_type: {}
graph:
  nodes:
    - id: review
      type: review_pipe
      inputs:
        approved_checkpoint_path: ${inputs.approved_checkpoint_path}
        repo_root: ${inputs.repo_root}
        base_ref: ${inputs.base_ref}
        task_type: ${inputs.task_type}
      params:
        review_diff_focused:
          argv: ["codex", "exec", "--model", "REPLACE_ME", "--", "cat"]
        review_wide_scan:
          argv: ["codex", "exec", "--model", "REPLACE_ME", "--", "cat"]
        review_test_focused:
          argv: ["codex", "exec", "--model", "REPLACE_ME", "--", "cat"]
        review_spec_conformance:
          argv: ["codex", "exec", "--model", "REPLACE_ME", "--", "cat"]
        review_spec_revision:
          argv: ["codex", "exec", "--model", "REPLACE_ME", "--", "cat"]
        write_checkpoint:
          checkpoint_dir: .nodeflow/checkpoints
          run_id: "003"
  final: review
